# homebrew-glide

Homebrew tap for [glide](https://github.com/matthewvilaysack/glide).

```sh
brew install matthewvilaysack/glide/glide
```

`Formula/glide.rb` is rewritten by the glide release workflow on every tagged release.
